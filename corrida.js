function simularCorrida (velocidade, tempo) {
    const velocidadeMax = 200;
    const tempoMax = 40;
    const aceleracao = 5;
    let velocidadeInicial = 0;
    let velocidade; 
    let tempo;

    if (velocidade < velocidadeMax && tempo < tempoMax) {
        tempo = 
    }
}